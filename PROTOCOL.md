# InkBLE 协议 v1

使用 BLE GATT，不使用蓝牙串口。所有多字节整数均为**无符号小端序**。一次一个连接；帧数据固定为 768×552 的四色图像，总长 **105984 字节**。

## 服务和特征

| 用途 | UUID | 属性 | 值长度 |
| --- | --- | --- | --- |
| 服务 | `cba00001-33c6-4d2e-a950-c61731a49a7e` | — | — |
| Control | `cba00002-33c6-4d2e-a950-c61731a49a7e` | Write | 5 或 19 |
| Data | `cba00003-33c6-4d2e-a950-c61731a49a7e` | Write | 9～244 |
| Status | `cba00004-33c6-4d2e-a950-c61731a49a7e` | Read、Notify | 20 |
| Info | `cba00005-33c6-4d2e-a950-c61731a49a7e` | Read | 16 |

Control、Data 使用 **Write With Response**。ATT 写成功只表示写入特征，应用层是否接受要读取 Status。固件请求 MTU 247，每个 Data 特征值上限为 `min(244, MTU-3)` 字节；默认 MTU 23 也可用，每包提供 12 字节图像。读取 Status 返回即时状态，通知可作为提示；发送器不依赖通知必达。

广播包含服务 UUID，扫描响应包含完整名称 `InkBLE-A1-XXXX` 或 `InkBLE-A0-XXXX`。无需配对/绑定，附近能够连接的客户端可以更新图片。

## 图像编码

以左上角为原点，从左向右、从上向下排列。一个字节按高位到低位包含 4 个像素：`p0<<6 | p1<<4 | p2<<2 | p3`。每行 192 字节，无头部、无行填充。例如连续黑白黄红四个像素编码为 `0x1B`。

| 索引 | 颜色 | 预览 RGB |
| --- | --- | --- |
| 0 | 黑 | 0, 0, 0 |
| 1 | 白 | 255, 255, 255 |
| 2 | 黄 | 232, 176, 0 |
| 3 | 红 | 200, 0, 0 |

发送方先应用 EXIF 方向，再比较原方向和顺时针旋转 90° 后等比缩放的占用面积，只有后者更大时才旋转一次，相同则不旋转；随后等比缩放、居中留白，使用 Atkinson 转换。颜色数值只用于预览与量化，屏幕物理颜色由颜料决定。驱动完成物理方向转换，客户端不要额外镜像或倒置行来补偿屏幕接线。

## Info：16 字节

Python 格式：`<4sBBHHIH`。

| 偏移 | 长度 | 含义 |
| --- | --- | --- |
| 0 | 4 | ASCII `EINK` |
| 4 | 1 | 协议版本 1 |
| 5 | 1 | 硬件版本：0=A0，1=A1 |
| 6 | 2 | 宽度 768 |
| 8 | 2 | 高度 552 |
| 10 | 4 | 帧字节数 105984 |
| 14 | 2 | 最大 Data 值长度 244，包含 8 字节包头 |

## Control

BEGIN：19 字节，Python 格式 `<BBIIIHHB`。

| 偏移 | 长度 | 含义 |
| --- | --- | --- |
| 0 | 1 | 操作码 1 |
| 1 | 1 | 协议版本 1 |
| 2 | 4 | 传输 ID，建议每次随机生成 |
| 6 | 4 | 图像长度 105984 |
| 10 | 4 | 整幅 2bpp 数据的 CRC32 |
| 14 | 2 | 宽度 768 |
| 16 | 2 | 高度 552 |
| 18 | 1 | 格式 1：上述四色 2bpp |

CRC32 使用标准 IEEE CRC32（反射多项式 `0xEDB88320`，初值和最终异或均为 `0xFFFFFFFF`），与 Python `zlib.crc32(payload)` 相同。

COMMIT：5 字节，`<BI>`，操作码 **2** + 传输 ID。只有完整长度和 CRC 正确才进入 Queued。重复 COMMIT 在 Queued、Refreshing、Done 状态下幂等，不会刷新两次。

ABORT：5 字节，`<BI>`，操作码 **3** + 传输 ID。取消尚未提交的接收；Queued/Refreshing 状态拒绝取消，确保丢失 COMMIT 应答后仍完成已经接受的图片。

Receiving 中再次收到相同 ID、CRC、长度、尺寸和格式的 BEGIN 会保留接收偏移，允许应答丢失后的重试；不同传输返回 Busy。Queued/Refreshing 也拒绝开始下一张图。

## Data

每包：`<II>` 传输 ID + 图像字节偏移 + 非空图像数据。按照 Status 的 `received` 位置依次写入。完全落在已接收范围内且字节一致的重复包可安全重发，不会重复计入 CRC；跳跃、内容不一致的重复包或跨越已接收边界的重叠包会被拒绝。

每隔约 4 KB 读取 Status，确认接收偏移和错误码。全帧发送后再发 COMMIT。

## Status：20 字节

Python 格式 `<BBBBIIII`。

| 偏移 | 长度 | 含义 |
| --- | --- | --- |
| 0 | 1 | 协议版本 1 |
| 1 | 1 | 状态 |
| 2 | 1 | 最近一次操作错误码，成功操作会清除 |
| 3 | 1 | 硬件版本 |
| 4 | 4 | 当前/最近传输 ID |
| 8 | 4 | 已收到图像字节数 |
| 12 | 4 | 固定帧字节数 105984 |
| 16 | 4 | 状态序号，变化时递增，允许回绕 |

| 状态值 | 名称 | 含义 |
| --- | --- | --- |
| 0 | Idle | 可开始传输 |
| 1 | Receiving | 正在接收 |
| 2 | Queued | 校验通过，等待显示任务 |
| 3 | Refreshing | 正在刷新屏幕，缓存不可修改 |
| 4 | Done | 屏幕完成刷新及关电，可开始下一张图 |
| 5 | Error | CRC/接收超时/屏幕超时，可重新 BEGIN |

错误码：0=无错误，1=无效控制命令，2=忙，3=格式不匹配，4=长度错误，5=传输 ID 错误，6=偏移错误，7=CRC 不一致，8=接收超时，9=屏幕 BUSY 超时，10=已取消。

普通无效请求不会破坏原传输。错误码属于最近的操作，不能只看错误码推断整个状态；客户端应同时检查状态和传输 ID。

## 连接生命周期

1. 扫描服务 UUID，连接后读 Info。
2. 若设备 Queued/Refreshing，等待结束。
3. BEGIN → 确认 Receiving、ID 正确。
4. 分包写 Data → 检查已接收偏移和错误码。
5. COMMIT → 等待同一 ID 的 Done，才报告“显示成功”。
6. 保持连接继续 BEGIN，或断开后下次重新连接。

Receiving 中断线会清空此次接收进度；超过 30 秒未收到有效 BEGIN/Data 则进入 Error。下次从头重传。Queued/Refreshing 中断线不会取消已提交图片，设备会完成显示并继续广播。

设备最多对每个 BUSY 等待阶段等待 45 秒。发送器默认等待整个刷新 180 秒。发送器超时/丢失响应不代表已提交图片停止刷新，应再次读取状态或查看屏幕。固件不保存图像文件，也不支持断电后的传输续传。
